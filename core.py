

def run(data_manager, metric):
    """Produce grid-search space and run evaluations"""
    X = data_manager.data
    y = data_manager.target

    # data_manager = DataManager(X=df, y=target, name=dataset, task='classification')

    # run(data_manager, metric)
